<div class="span6 offset3">

<div class="row-fluid">
<% if current_user.admin? %>
<%= render 'shifts_admin_menu' %>
<% end %>
</div>

<% @offers_count = @shift.shift_requests.where(worker_status: nil).where(manager_status: true).length %>
<h3><%= @shift.role %> @ <%= Firm.find(@shift.firm_id).name %></h3>
<i>Click on available workers to offer them this shift.</i><br>


  <%= render 'shared/available_users' %>

</div>