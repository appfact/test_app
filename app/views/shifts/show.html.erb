<div class="container">
	<div class="nav pull-right"><%= link_to "Back to shifts page", '/shifts', 
                                class: "btn btn-medium btn-primary" %>
                            </div>
</div>
<%= @shift.id %> - <%= @shift.role %><br>
Posted by <%= @shift.user.name %><br>

<br>
<%= render 'shared/stats' %><br>
<%= render 'request_form' if signed_in? %>