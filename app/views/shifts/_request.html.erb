<% if @shift.allocation_type == 1 %>

Shift is available first come first served <%= link_to "Get Shift", user_request_shift_path(@shift), class: "btn btn-primary btn-medium" %>

<% else %>

Shift is available <%= link_to "Request Shift", user_request_shift_path(@shift), class: "btn btn-primary btn-medium" %>

<% end %>